box: python:2.7-slim
dev:
  steps:
    - pip-install
    - internal/watch:
        code: python app.py
        reload: true
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `pip install` command
    - pip-install

    # A custom script step, name value is used in the UI
    # and the code value contains the command that get executed
    - script:
        name: python unit test
        code: |
          echo "python version $(python --version) running"
          python app_test.py
deploy:
  steps:
    - pip-install
    - internal/docker-push:
        username: $DOCKERHUB_USERNAME
        password: $DOCKERHUB_PASSWORD
        tag: pyapp-test
        cmd: python /pipeline/source/app.py
        ports: "5000"
        repository: $DOCKERHUB_REPO
        registry: https://registry.hub.docker.com
# add pipline
run_on_occs:
  steps:
    - script:
        name: create deployment on occs
       code: |  
          echo "Get auth token*********************"
		  AUTH_TOKEN=$(curl -k "https://140.86.32.204/api/auth" -d '{"username":"admin","password":"Welcome1#"}' | python -c "import sys, json; print json.load(sys.stdin)['token']")
		  echo $AUTH_TOKEN